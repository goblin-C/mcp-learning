# Cognito MCP Server

This MCP server generates AWS Cognito CloudFormation templates.

It is used during project setup when developers run:

auth:configure

The MCP asks configuration questions and generates:

resources/Cognito-Configuration.yaml